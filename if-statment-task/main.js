    // task1 
    let age= 22;

    if(age>18)
        console.log('you are an adult');

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 2 

    let score=45;
    if(score<=50)
        console.log('you failed the test');

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 3

    let name='jhon'

    if(name ==='jhon')
        console.log('hello, jhon');


    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 4

    let day='monday';

    if(day !== 'sunday' && day!=='sutrday')
        console.log('is a weekday');

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 5

    let num=4;

    if(num%2 == 0)
        console.log('is even number');

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 6

    let char='a'
    if(char.length==1){
    if (char >= 'A' && char <= 'Z' || char >= 'a' && char <= 'z') {
        console.log( 'is letter');
    }
    }

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 7 

    let list=[1,2,3,4,5,6,7]
    
    if(Array.isArray(list))
        console.log('is an array');


    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 8

    let x=5 ; 
    if(x>0)
        console.log('is a positive number');


    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 9   

    let y=-5 ; 
    if(y<0)
        console.log('is a negative number');


    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 10

    let z=12;

    if(z%3==0)
        console.log('is a multiple of 3');

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 11

    let gpa=3.5;

    if(gpa>= 3.0)
        console.log('congratulations, you have a good GPA!');



    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 12




    let passw='mypeeeee45'

    if(passw.length>=8)
        console.log('your password is a strong');


    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 13

let age1=30 ;

if(age1 >= 18 && age1 <=65 )
    console.log('you are in the work age');

    // &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

    //  task 14

let color ='red'
if(color=='red' || color == 'blue' || color == 'green')
    console.log('is a primary number');

